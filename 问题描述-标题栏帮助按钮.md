# 问题描述：Electron 标题栏"?"帮助按钮，真实用户点击无效

## 目标
在 Electron 桌面应用（DSH Desktop）窗口**标题栏右上角、系统最小化按钮的左边**，放一个"?"帮助按钮。点击后在其**正下方**弹出 HTML 下拉菜单，含两项：「检查更新」「关于 DSH Desktop」，点击菜单项执行对应功能（弹系统对话框）。

## 实现方案（当前 v1.1.4）
- `BrowserWindow`：`titleBarStyle: 'hidden'` + `titleBarOverlay: { color, symbolColor, height: 36 }` —— **保留系统原生最小化/最大化/关闭按钮**（右上角），其余标题栏区域自绘
- 主窗口 webContents 加载 `titlebar.html`（36px 高自绘标题栏）：左侧 D 图标+标题+版本号，右侧「?」按钮（`-webkit-app-region: no-drag`，距窗口右缘 148px 处，即系统按钮左侧）
- `WebContentsView` 加载 DSH 应用页面，`setBounds({ x:0, y:36, width, height-36 })`
- 「?」按钮点击 → JS 打开 `#menu`（HTML 下拉菜单，`position:fixed`，left/top 用按钮 `getBoundingClientRect()` 计算 → 菜单左缘=按钮左缘、顶部=按钮下缘+4px）
- 菜单项点击 → preload 的 `ipcRenderer.send('tb:menu-action', action)` → 主进程执行功能
- 打包：electron-builder portable exe；Electron 33.4.11；Windows 11

## 环境
- Windows 11，双屏：主屏 3840x2160（150% 缩放，逻辑 2560x1440）；副屏 2560x1440
- 用户同时运行着另一个旧版实例（v1.1.2，系统原生标题栏），测试实例用独立端口+独立 userData

## 已验证的事实（重要）
1. **CDP 合成点击**（`Runtime.evaluate` 执行 `btn.click()`）：菜单能打开 ✓
2. **CDP `Input.dispatchMouseEvent`**（mouseMoved→mousePressed→mouseReleased，Chromium 内部真实鼠标事件链）：点击「?」→ 菜单打开 ✓；点击菜单项 → 主进程收到 IPC（日志确认）✓
3. **WM_NCHITTEST**（用屏幕坐标查询窗口）：「?」按钮位置返回 **HTCLIENT**（可点击区域，非拖拽区）；标题栏空白处返回 HTCAPTION（拖拽区）✓
4. 早期版本（v1.1.2 第一版，同样的 titlebar.html + titleBarOverlay，但点击走 `ipcRenderer.send` → 主进程 `Menu.popup` 弹**原生菜单**）：**用户真实点击有效**（用户截图确认菜单弹出，只是位置不对）——证明"真实鼠标点击能到达该按钮、能触发事件"这个链路本身是通的
5. 渲染层面：CDP 截图中按钮、菜单、位置全部正确

## 问题现象
**用户真实鼠标点击「?」按钮，菜单不弹出（无任何反应）**。但上述所有 CDP 层验证均通过。

## 关键矛盾点（供分析）
- CDP `Input.dispatchMouseEvent`（Chromium 内部派发）有效 → 说明 webContents 内的事件处理、按钮监听、菜单逻辑都没问题
- WM_NCHITTEST 返回 HTCLIENT → 系统层面判定该区域可点击
- 早期版本真实点击有效（走 IPC + 原生菜单）→ 说明真实点击能到达按钮
- **但现在的 HTML 菜单版真实点击无效** → 差异在哪？
  - 早期版：按钮 click → `ipcRenderer.send`（IPC 到主进程，弹原生菜单——菜单由**主进程/系统**绘制，**不依赖 titlebar.html 的 DOM**）
  - 现在版：按钮 click → **操作 titlebar.html 自身的 DOM**（显示 `#menu`）
  - 推断方向 A：真实点击到达了按钮、click 触发了，但**菜单 DOM 显示了却被裁剪/遮挡/看不见**（曾发现过 `body { overflow:hidden }` 会裁剪下拉菜单的坑，v1.1.4 已改为 `html overflow:hidden + body overflow:visible`，但仍需验证打包后实际 CSS 是否生效）
  - 推断方向 B：真实点击**没有**到达 webContents（titleBarOverlay 模式下的鼠标事件拦截问题），而早期版"有效"的结论可能是用户点到了**另一个窗口**（当时测试窗口与用户窗口重叠，用户截图时点的是测试窗口还是自己的窗口无法 100% 确认——**该"早期有效"证据需打问号**）
  - 推断方向 C：`-webkit-app-region: no-drag` 在 titleBarOverlay 模式下未生效，点击被当作标题栏拖拽（WM_NCHITTEST 显示 HTCLIENT 与之矛盾，但 WM_NCHITTEST 是 SendMessage 查询的静态结果，真实鼠标路径可能不同）

## 已做但未成功的排查
- 用 `SetCursorPos + mouse_event` 模拟真实点击：结果不可靠（测试窗口与用户窗口重叠时点击落错窗口；且主屏 DPI 150%，坐标换算需注意），曾被误判为"按钮点击被系统拦截"
- 曾注入 `mousedown/mouseup/click` 计数探针，真实点击后计数为 0 —— 但那次测试窗口与用户窗口重叠，点击可能落在用户窗口上，**结论不可信**

## 请求分析的切入点
1. titleBarOverlay + titleBarStyle:'hidden' 模式下，鼠标点击事件在 Windows 上是否会经过 Electron 的 hit-test 后被吞掉（对比 frame:true 普通窗口）
2. titlebar.html 的 `body overflow:visible` 是否真的让 fixed 定位菜单在 36px 高的 html 外可见
3. 如何可靠地验证"真实点击是否到达 webContents"（比如给 titlebar.html 加全局 mousedown 计数，用**不与用户窗口重叠**的测试窗口验证）
4. 若确认是事件被吞：备选方案（frame:false 全自绘含窗口按钮 / 完全放弃标题栏按钮改为页面内按钮）

---

# 结论（v1.1.5 已修复，2026-08-15 验证）

## 根因（已用实验证实）

**真实点击链路完全正常，菜单也正常弹出——但它被 `WebContentsView` 物理遮挡，用户看不见。**

- `BrowserWindow` 自身 webContents（titlebar.html）在窗口 Z 序的**最底层**；`WebContentsView`（DSH 页面）是**原生子视图，盖在其上方**（y≥36px 全部区域）。
- titlebar.html 里的 `#menu` 是 `position:fixed`，弹出位置 top≈40px（按钮下缘+4），**正好落在 WebContentsView 的区域内** → 菜单渲染在 titlebar 的合成层里，但被 DSH 页面盖住，屏幕上不可见。
- CDP 截图/点击都作用于 titlebar webContents **自身的合成输出**（含被遮挡的部分），所以全部"验证通过"；真实用户看到的是屏幕合成结果 → "无反应"。
- 证据（150% DPI 实测，像素采样）：CDP 截图中菜单中心为 `#FFFFFF`（浅色菜单），**真实屏幕同一位置为 `#101220`（DSH 页面背景色）**——菜单确实被页面盖住。
- 这也解释了 v1.1.2"原生菜单有效"：`Menu.popup` 是系统顶层窗口，永远在一切视图之上。
- `-webkit-app-region`/WM_NCHITTEST/事件吞没 等方向均被排除（WM_NCHITTEST 返回 HTCLIENT 本来就是对的）。

## 修复方案：菜单改为独立子窗口（helpmenu.html）

标题栏按钮点击 → IPC 到主进程 → 主进程弹出**无边框、透明、`focusable:false` 的独立 BrowserWindow**（`parent: mainWindow`），显示与原先完全相同的 HTML 菜单（浅/深色主题跟随），菜单位置=按钮左缘、按钮下缘+4px（DIP 计算，无 DPI 误差）。

- 菜单项点击 → `helpmenu-preload.js` 的 `hm:action` IPC → 主进程执行「检查更新」「关于」并关闭菜单
- 点击 DSH 页面任意处 → 关闭菜单（`input-event` 监听 mouseDown；注意 `before-input-event` 在此 Electron 版本不派发鼠标事件）
- 标题栏空白处 / Esc → 关闭菜单
- 主窗口移动/缩放 → 菜单自动跟随（owned window 特性 + 50ms 防抖重定位）
- 菜单窗口不抢焦点（`focusable:false`），主窗口悬停态、原生按钮交互不受影响

## 改动文件
- `main.js`：`openHelpMenu/closeHelpMenu/repositionHelpMenu` + `tb:help-toggle`、`hm:action` IPC + `input-event` 关闭逻辑（新增约 90 行）
- `titlebar.html` / `titlebar-preload.js`：移除被遮挡的 DOM 菜单，按钮点击改走 `helpToggle()`
- `helpmenu.html` / `helpmenu-preload.js`：新增（独立菜单窗口）
- `package.json`：build.files 加入两个新文件

## 验证（真实 `mouse_event` 点击 + 屏幕像素采样，测试窗口与用户 v1.1.2 窗口不重叠）
- [x] 点击「?」→ 菜单弹出且屏幕上可见（位置=按钮正下方）
- [x] 点击菜单项 → 主进程收到 IPC 并弹系统对话框
- [x] 点击「?」再次 → 关闭；点击 DSH 页面 → 关闭；Esc → 关闭
- [x] 移动/缩放主窗口 → 菜单跟随重定位
- [x] 30 秒静置无幽灵事件
- [x] 多实例（v1.1.2 + v1.1.5 独立 userData 同屏运行）互不干扰
